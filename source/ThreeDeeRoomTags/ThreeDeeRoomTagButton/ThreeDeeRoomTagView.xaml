<Window
    Name="Win"
    x:Class="ThreeDeeRoomTags.ThreeDeeRoomTagButton.ThreeDeeRoomTagView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:classes="clr-namespace:ThreeDeeRoomTags.Classes"
    mc:Ignorable="d"
    Height="620"
    Width="400" 
    MinHeight="620"
    MinWidth="400"
    WindowStartupLocation="CenterScreen"
    Style="{DynamicResource CustomWindowStyle}" 
    Background="#FF232323"
    Title="{Binding PluginVersion}">
    <Window.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="b2v" />
            <classes:BooleanAndConverter x:Key="booleanAndConverter"/>
            <Storyboard x:Key="WiggleStoryboard" FillBehavior="Stop">
                <DoubleAnimation Storyboard.TargetName="transform"
                                 Storyboard.TargetProperty="Angle"
                                 From="5"
                                 Duration="0:0:1"
                                 RepeatBehavior="0:0:1">
                    <DoubleAnimation.EasingFunction>
                        <ElasticEase EasingMode="EaseOut"
                                     Oscillations="5"
                                     Springiness="2" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ThreeDeeRoomTags;component/ThemesFolder/ColourfulDarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </Window.Resources>
    <Grid Margin="8" Name="MainGrid">

        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="{Binding TitleText,FallbackValue=3d Room Tags}" FontSize="32" FontFamily="Segoe UI Black"/>

        <TabControl Grid.Row="1">
            <TabItem Header="3d Tag Creation">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="14"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="Target Spatial Elements:" FontSize="14"/>
                    <ComboBox Name="TargetComboBox" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" Height="32" SelectedIndex="{Binding TargetIndex, Mode=TwoWay, FallbackValue=0}" SelectionChanged="TargetChanged" >
                        <ComboBoxItem Content="Rooms"/>
                        <ComboBoxItem Content="Spaces"/>

                    </ComboBox>

                    <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Get Spatial Elements from Link?" FontSize="14"/>
                    <Grid Grid.Row="3" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Name="FromLinkCheckbox" IsChecked="{Binding FromLink, FallbackValue=False}" IsEnabled="{Binding Links.Count}" VerticalAlignment="Center" Checked="ToggleButton_OnChecked" Unchecked="ToggleButton_OnChecked">
                            <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5" />
                            </CheckBox.LayoutTransform>
                        </CheckBox>
                        <ComboBox Name="LinkComboBox" Grid.Column="1" SelectedIndex="{Binding LinkIndex, Mode=TwoWay}" ItemsSource="{Binding Links}" Visibility="{Binding FromLink, Converter={StaticResource b2v}}" DisplayMemberPath="Name" SelectionChanged="Link_OnSelectionChanged"  HorizontalAlignment="Stretch"/>
                    </Grid>

                    <Label Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"  Content="Select Phase:" FontSize="14"/>
                    <ComboBox Grid.Row="5" Grid.Column="1" Name="PhaseComboBox" HorizontalAlignment="Stretch" Height="32" ItemsSource="{Binding Phases}" DisplayMemberPath="Name" SelectionChanged="Selector_OnSelectionChanged"/>

                    <Label Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Content="3d Spatial Tag Family:" FontSize="14"/>
                    <ComboBox Grid.Row="7" Grid.Column="1" Name="FamilySymbolComboBox" SelectedIndex="{Binding FamilySymbolIndex, Mode=TwoWay}" HorizontalAlignment="Stretch" Height="32" ItemsSource="{Binding RoomTagFamilySymbols}" SelectionChanged="FamilySymbolSelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0}: {1}">
                                            <Binding Path="FamilyName"/>
                                            <Binding Path="Name"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <Label Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Content="Update Previous Spatial Tags?*" FontSize="14" ToolTip="SUGGESTED TO LEAVE CHECKED. If enabled, this will try to update the previously created room tags, (This is achieved by looking up the room id parameter on the included 3d room tag family)."/>
                    <CheckBox Grid.Row="9" Grid.Column="1" IsChecked="{Binding UpdateExisting, FallbackValue=True}">
                        <CheckBox.LayoutTransform>
                            <ScaleTransform ScaleX="1.5" ScaleY="1.5" />
                        </CheckBox.LayoutTransform>
                    </CheckBox>

                    <Border Grid.Column="1" Grid.Row="10">
                        <Border.RenderTransform>
                            <RotateTransform x:Name="transform" CenterX="100"/>
                        </Border.RenderTransform>
                        <TextBlock Text="{Binding FlyOutText, NotifyOnTargetUpdated=True, FallbackValue=''}" IsHitTestVisible="True" TextWrapping="Wrap">
                            <TextBlock.Triggers>
                                <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                    <BeginStoryboard Storyboard="{StaticResource WiggleStoryboard}"/>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="TextBlock.MouseDown">
                                    <BeginStoryboard Storyboard="{StaticResource WiggleStoryboard}"/>
                                </EventTrigger>
                            </TextBlock.Triggers>
                        </TextBlock>
                    </Border>
 
                    <Border Grid.Column="1" Grid.Row="11">
                        <Border.RenderTransform>
                            <RotateTransform x:Name="transform2" CenterX="100"/>
                        </Border.RenderTransform>
                        <TextBlock Foreground="Red" Text="{Binding UnboundedFlyOutText, NotifyOnTargetUpdated=True, FallbackValue=''}" IsHitTestVisible="True" TextWrapping="Wrap">
                            <TextBlock.Triggers>
                                <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                    <BeginStoryboard Storyboard="{StaticResource WiggleStoryboard}"/>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="TextBlock.MouseDown">
                                    <BeginStoryboard Storyboard="{StaticResource WiggleStoryboard}"/>
                                </EventTrigger>
                            </TextBlock.Triggers>
                        </TextBlock>
                    </Border>
                </Grid>

            </TabItem>
            <TabItem Header="Settings">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="36"/>

                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="Text Height (in model units):" FontSize="14"/>
                    <TextBox Grid.Row="1" Text="{Binding TextHeightString, Mode=TwoWay}">

                    </TextBox>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="14"/>
            </Grid.ColumnDefinitions>
            <Image Source="/ThreeDeeRoomTags;component/Resources/DesignTechUnraveledMark_512x383.png" HorizontalAlignment="Left" Margin="4" MouseDown="UIElement_OnMouseDown" Cursor="Hand"/>
            <Button  Margin="4" Grid.Column="2" Content="Close" Command="{Binding Close, Mode=OneWay}" CommandParameter="{Binding ElementName=Win}"/>
            <Button Margin="4" Grid.Column="3" Content="Create/Update Tags" Command="{Binding Run, Mode=OneWay}" CommandParameter="{Binding ElementName=Win}" IsEnabled="{Binding Rooms.Count}">

            </Button>
        </Grid>
    </Grid>
</Window>
